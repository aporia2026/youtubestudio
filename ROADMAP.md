# Roadmap

Source of truth for what's planned, in flight, and shipped. **Update this file in the same commit that ships a feature** — new sessions read it before starting work to avoid duplicating shipped items or renaming phases.

## Origin

This roadmap was written during the **May 1, 2026 system audit** (response to "extreme robust insane review" prompt). The four-phase structure was committed to then; sub-items inside each phase have evolved as work progressed.

A more detailed file-by-file plan exists for Phase 1 in [PHASE1_PLAN.md](PHASE1_PLAN.md). Phases 2–4 were planned in conversation and tracked here.

## Status legend

- ✅ Shipped (in production, migration applied to prod DB)
- 🚧 In flight
- ⏸ Not started
- ❌ Cancelled (with reason)

---

## Phase 1 — Foundation ✅

Auth + multi-tenancy + encryption at rest + CI + rate-limit + observability + test harness. See [PHASE1_PLAN.md](PHASE1_PLAN.md) for the detailed plan that drove this phase.

Migrations: **0001–0015** (rate limits) + **0016** (channels brand kit, started Phase 2).

## Phase 2 — Multi-channel ergonomics ✅

| PR | Topic | Status |
|---|---|---|
| #1 | Channel switcher in top bar | ✅ |
| #2 | Per-channel brand kit + auto-pipe into script gen + QA | ✅ |
| #3 | YouTube Analytics ingestion | ✅ |
| #4 | Dashboard rebuild — "what needs your attention today" | ✅ |
| #5 | Workspace scoping on top routes + CI lint gate | ✅ |
| Hardening | Post-Phase-2 code review fixes (P0 channel UNIQUE + P1s) | ✅ |

Migrations: **0016–0019**.

## Phase 3 — 2026 table-stakes features ✅

| PR | Topic | Migration | Status |
|---|---|---|---|
| #1 | Auto-dubbing pipeline (translation + ElevenLabs multilingual) | 0020 | ✅ |
| #2 | Shorts pipeline (script-to-Short extractor + voiceover) | 0021 | ✅ |
| #3 | Veo 3 / Sora 2 B-roll per shot (Production Doc per-row) | 0023 | ✅ |
| #4 | Native YouTube A/B title + thumbnail (videos.update + snapshots) | 0024 | ✅ |

Notes:
- Migration **0022** (production_doc_styles) shipped in parallel, not part of Phase 3.
- Shorts → MP4 render (Remotion 1080×1920) was **deferred** from PR #2 — see Beyond Phase 4 below.

## Phase 4 — Innovative differentiation ✅

| Sub | Topic | Status | Notes |
|---|---|---|---|
| 4.1 | **Court of Critics live** — stream the existing 4-phase panel as it runs + persist full deliberation transcript + real-time courtroom UI | ✅ | Migration 0025. AsyncGenerator wrapper at [src/lib/script-critics/runner-live.ts](src/lib/script-critics/runner-live.ts) reuses the original normalizers; SSE route streams + persists each event; `/critics` page renders critic cards filling in as drafts complete. |
| 4.2 | **Retention-curve predictor** — predict viewer drop-off shape from a script before publish | ✅ | Migration 0026. RAG over `video_analytics.retention_curve` (workspace's own past videos), scopes by channel when possible. Outputs predicted curve + per-segment drop forecast + cross-segment fixes. AVP recomputed from curve (LLMs misintegrate). UI at `/retention`. |
| 4.3 | **Fix-the-dip** — detect retention drops in published videos + suggest specific script/edit fixes at the timecodes that drop | ✅ | Migration 0027. Two-stage: deterministic dip detection (sliding window over `video_analytics.retention_curve`, no LLM) → AI alignment of each dip to script section + per-dip fix + cross-cutting patterns. UI at `/fix-the-dip` with curve overlay marking each dip. |
| 4.4 | **Cross-channel cannibalization detector** — same niche + same audience + competing uploads warning | ✅ | Migration 0028. Pulls scheduled + recently-published uploads across the workspace, finds cross-channel pairs in a ±7-day window, scores Jaccard similarity over significant title tokens, top-10 above threshold get an AI explanation + concrete fix. UI at `/cannibalization` with dismiss. |
| 4.5 | **"Ask Studio" agent over your own DB** — natural-language query of analytics + project state | ✅ | Migration 0029. Anthropic native tool-use loop over a curated 9-tool read-only catalog (channels, recent/top/underperforming videos, scheduled items, projects, A/B tests, per-channel upload counts, single-video deep-dive). Every executor auto-scopes to workspace_id. Hard caps: 6 iterations, 50 rows/tool. UI at `/ask-studio` with suggested prompts + collapsible tool trace. |
| 4.6 | **Mobile narrator PWA** — installable, offline-capable narrator portal | ✅ | Service worker scoped to `/narrator/` (cache-first static, stale-while-revalidate HTML, network-first API, never caches non-GET so uploads always hit network). Manifest + iOS web-app meta + safe-area inset support + install pill (Android beforeinstallprompt) + iOS A2HS hint with localStorage dismissal. Offline fallback page. |
| 4.7 | **Slack/Discord webhooks** — send events (A/B winner, cannibalization, panel completed, retention dip) to an ops channel | ✅ | Migration 0030. Per-workspace subscriptions (Slack/Discord/generic) with per-event filters; URLs encrypted at rest, only `url_preview` exposed; URL validation gates host (hooks.slack.com / discord.com / discordapp.com) + blocks loopback/private addrs. Fire-and-forget dispatcher with full delivery audit log. Wired producers: A/B test conclude + high-risk cannibalization. Test-webhook button. |

## Phase 5 — Operational depth ✅

Promoted from "Beyond Phase 4" parking lot on 2026-05-02 after Phase 4 fully shipped. Theme: features that deepen day-to-day operability of the system Phase 1–4 built.

| Sub | Topic | Status | Notes |
|---|---|---|---|
| 5.1 | **Comment & community management** — pull YouTube comments per channel, AI-triage by intent, reply + moderate from inside the app | ✅ | Migration 0031. Sync via public Data API (cheap), AI triage via Haiku into 8-intent enum (question/support/fan/feedback/troll/spam/self_promo/other) with suggested-reply per comment, reply + hold/reject via channel OAuth. UI at `/comments` with intent-pill counts, video filter, unreplied filter, restore-AI-suggestion button. Pinning skipped (not in YouTube public API as of May 2026). |
| 5.2 | **Workflow triggers** — "when CTR drops below X, auto-create a fix-the-dip"; "when A/B concludes, auto-snapshot 7 days later" | ✅ | Migration 0032. Rules = (event, condition, action, delay). Producers fire on ab_test_concluded / cannibalization_high_risk / video_analytics_synced. Conditions are a small DSL (lt/gte/equals/in/exists/all/any) over event payload. Hourly Vercel cron drains the queue (CRON_SECRET-gated). 4 actions: sync_video_analytics, run_dip_analysis, run_cannibalization_scan, send_webhook_event. UI at `/workflows` with rule builder + recent action runs. |
| 5.3 | **Competitor intelligence dashboard** — cross-channel signals on top of the existing per-competitor `/competitors` page | ✅ | No new migration. New aggregator at [src/lib/competitor-summary.ts](src/lib/competitor-summary.ts) (PERCENTILE_CONT for 30d view-count median, single-query per-channel cadence + momentum). New page `/competitors/dashboard` with KPI strip, recent breakouts (≥ 2.5× channel median, last 14d), per-channel momentum table, accelerating/stalled callouts. Compact `CompetitorSignalsCard` lifted into the main `/dashboard`. |
| 5.4 | **Spend tracker** — per-API per-project AI cost log | ✅ | Migration 0033. Pricing registry in `src/lib/ai-pricing.ts` (Anthropic + OpenAI + Google direct + Kie market routes; cached-input discount). `generateText` accepts an optional `spend` context — Anthropic/OpenAI/Google branches capture token usage from SDK responses + fire-and-forget log. Wired callers: retention_predictor / fix_the_dip / comment_triage. UI at `/spend` with KPI strip, daily bar chart, by-feature/by-model/by-project breakdowns, top-10 most-expensive calls. |
| 5.5 | **Shorts → MP4 render (Remotion 1080×1920)** — closes the Shorts loop (extract → voiceover → render → publish) | ✅ | No new migration (reuses existing `render_jobs` table + `shorts.rendered_video_url` column from migration 0021). New Remotion composition `ShortVideo` registered in [src/remotion/Root.tsx](src/remotion/Root.tsx) — 1080×1920, gradient bg, title chip, large center captions with last-word accent, optional channel pill, ElevenLabs voiceover. Pure helpers in [src/lib/shorts-render.ts](src/lib/shorts-render.ts) (sentence-aware caption splitter, config builder). New route `/api/render/short` mirrors the existing `/api/render/video` orchestration (Remotion bundle + renderMedia + Vercel Blob upload + persist `rendered_video_url`). UI integration on `/shorts` with self-contained progress bar + inline player. |

## Phase 6 — Polish ✅

Promoted from "Beyond Phase 5" parking lot on 2026-05-03. Theme: harden the surfaces shipped in Phases 4-5 — observability, configurability, and error handling — without adding new user-facing features.

| Sub | Topic | Status | Notes |
|---|---|---|---|
| 6.1 | **Spend instrumentation sweep** — wire `spend` context through every server-side `generateText` call so `ai_spend_log` captures all 30+ AI calls, not just the 3 starter ones | ✅ | No new migration. Threaded `AiSpendContext` through 20+ routes and lib functions (script-critics charter/draft/deliberation/chair, video composer stages, cannibalization, dubbing, shorts, comment triage, etc.). New helper `makeSpendContext()` in [src/lib/ai-spend.ts](src/lib/ai-spend.ts) for raw POST routes that lack a session in scope. Per-stage `featureArea` labels (e.g. `critic_panel_live_deliberation_hook-coach`) so the spend dashboard can attribute cost to the exact phase that ran it. Commit `e7ba3c7`. |
| 6.2 | **Per-workspace AI model defaults** — workspace/section/feature override system replacing localStorage flat map | ✅ | Migration 0034. Three resolver tiers (workspace → section → feature → hardcoded fallback) with 5s per-workspace cache. `APP_FEATURES` expanded from 9 to 28 entries — every Phase 4/5 surface now declares its section + hardcoded default. Settings UI at `/settings?section=models` rebuilt as three stacked cards (Workspace default / By section / Per-feature accordion grouped by section), each picker reuses the search-enabled `ModelSelector`. Server side: `getEffectiveModelId(workspaceId, feature)` wired into all 7 lib functions (cannibalization, ask-studio, comment-triage, fix-the-dip, retention-predictor, shorts, dubbing) + 4 routes that previously hardcoded model literals. Background jobs honour the user's overrides — not just foreground UIs. 14 tests cover scope codec round-trip + four-tier precedence + catalogue invariants. Commits `45e11c3` / `12a95ae` / `62a33af`. |
| 6.3 | **Error-path hardening sweep** — replace 16 copy-pasted catch-and-leak blocks with a single typed helper | ✅ | No new migration. New `domainErrorResponse(err, opts)` helper in [src/lib/route-helpers.ts](src/lib/route-helpers.ts) with two-tier classification: known patterns → log at WARN + pass-through (4xx, user-facing); unknown → log at ERROR with full detail + return generic message (5xx). Applied across 16 Phase 4/5 action routes (ab-tests×4, ask-studio, cannibalization, comments×4, retention×2, shorts/voiceover, webhooks, workflows×2). Net wins: no more `err.message` leaks in 5xx, every error path is logged for operability, status codes are now consistent (404 not-found, 400 validation, 502 upstream/AI). 5 new tests cover the helper. Commits `0e27538` / `46369e4`. |

## Phase 7 — Publishing pipeline ✅

Promoted on 2026-05-03 after Phase 6 shipped. Theme: close the production loop — let the user upload a finished video to YouTube directly from the app, without leaving for YouTube Studio.

| Sub | Topic | Status | Notes |
|---|---|---|---|
| 7.1 | **Publishing pipeline — migration + lib** — `published_videos` table tracking upload lifecycle (queued / uploading / processing / live / failed), thumbnail upload, playlist add. Lib wraps the YouTube `videos.insert` + `thumbnails.set` + `playlistItems.insert` flow with per-step error handling | ✅ | Migration 0035. Pure helpers in [src/lib/publishing-types.ts](src/lib/publishing-types.ts) (validatePublishRequest with YouTube limits, snippet/status builders, state machine, URL builder) — 33 tests. Orchestrator in [src/lib/publishing.ts](src/lib/publishing.ts) reuses `getValidAccessToken` and `uploadThumbnailOAuth` rather than re-implementing OAuth refresh + thumbnail upload. videos.insert wired via `multipart/related` (single-request — fits ≤256MB; resumable can come later if users hit the cap). Thumbnail + playlist add are best-effort: failures get appended to error_message but don't fail the publish. `pollPublishStatus(id, workspaceId)` flips processing → live by polling videos.list. `pollAllPendingPublishes()` drains all 'processing' rows for the cron + fails rows stuck > 1h. |
| 7.2 | **Publishing API routes + UI on schedule item / project detail** — POST `/api/publishing/upload` (kicks off upload), GET `/api/publishing/status/[id]` (polls for live status), DELETE for cancel. UI on `/schedule/[id]` and `/projects/[id]` with a "Publish to YouTube" button that opens a modal: title / description / tags / category / privacy / playlist / publishAt timestamp / thumbnail. Live status badge once upload starts | ✅ | POST/GET `/api/publishing` and GET `/api/publishing/[id]` (auto-polls YouTube once when status='processing' so a single fetch always returns the freshest signal). Hourly cron at `/api/cron/poll-publishing` drains pending rows + fails > 1h-stuck (wired in vercel.json with same CRON_SECRET pattern as run-workflows). [PublishToYoutubeModal](src/components/publishing/PublishToYoutubeModal.tsx) — full form with channel auto-pick when one OAuth-connected channel exists, status panel polls every 5s once submitted. Surfaced from `ItemDetail` (schedule) next to "Prepare for YouTube" with all metadata pre-filled, and from `/projects/[id]` action row. **Per the menu-organization rule: no new top-level nav entry — Publish is an action surfaced from the contexts where you already have a finished video.** |
| 7.3 | **Wire `video_published` event into workflow trigger system** — fires when 7.1 sets status = live, lets workflows react ("when video published, post to Slack", "when video published, schedule a 7-day analytics snapshot") | ✅ | `video_published` added to both `WORKFLOW_TRIGGER_EVENTS` (with payload_fields: publish_id, youtube_video_id, youtube_url, channel_db_id, project_id, schedule_item_id, title, privacy_status) and `WEBHOOK_EVENT_TYPES`. `emitVideoPublishedEvent(id, workspaceId)` fired fire-and-forget from `pollPublishStatus` when status flips to 'live' — lazy imports for webhooks + workflows to keep deps out of consumers. Mirrors the ab-tests / cannibalization producer pattern. Two new tests assert both registries include the event so a future regression is caught. |

## Phase 8 — Audit-driven hardening + new theme ✅

Promoted on 2026-05-03 after a multi-agent audit of Phases 1–7 (security, data-integrity, errors/observability, performance, frontend) surfaced 30 findings. Phase 8.1–8.4 closed the audit out across 11 commits on `phase-1-foundation`. 8.5 closed the AI prediction → outcome learning loop on the retention predictor. 8.6 is the second-pass hardening triggered by a four-agent QA review of Phase 8.1–8.5 — found 1 P0 (SSRF DNS rebinding) + 16 P1s, all closed across five sub-commits.

**Migration map (Phase 8):** 0037 (composite indexes), 0038 (competitor_channels workspace UNIQUE), 0039 (published_videos idempotency_key), 0040 (prediction_outcomes table).

| Sub | Topic | Status | Notes |
|---|---|---|---|
| 8.1 | **Cross-tenant data leak P0s closed** — three previously-unauthenticated routes were leaking across workspaces or burning AI tokens for anonymous callers | ✅ | Audit C1 + C2 + C3. /api/projects/[id]/scripts (GET+POST) was bare with no auth and no workspace filter — anyone who guessed a project UUID could dump every script in the DB. /api/competitors (GET, POST, [id] GET/DELETE, sync, analyze, ideas, thumbnail-analyze, video-analyze, video-analyze-batch — eight routes total) returned every workspace's competitors in one query. /api/generate/ideas was only IP-rate-limited so anonymous traffic could burn AI tokens. All wrapped in apiRoute.authed with workspace_id filters; cross-workspace ids return 404 (not 403, no existence leak). Migration 0038 supports the competitor fix by replacing the legacy global UNIQUE(channel_id) on competitor_channels with workspace-scoped UNIQUE(workspace_id, channel_id). |
| 8.2 | **Publishing pipeline hardening** — SSRF block on source/thumbnail fetches + idempotency-key support + state-machine compare-and-swap guards + streaming MP4 upload | ✅ | Audit C4 + C5 + C7 + M10 + M12. New url-safety.ts helper (53 tests) blocks every documented private/loopback/link-local IPv4 range, IPv6 private ranges (including IPv4-mapped both compressed and dotted forms), AWS IMDS, *.internal/.local — wired into both publishing's source-video fetch and webhooks' generic-kind URLs. Migration 0039 adds idempotency_key to published_videos with partial UNIQUE(workspace_id, idempotency_key) — POST /api/publishing reads `Idempotency-Key` header, retries with the same key return the existing row instead of creating duplicate uploads. State-machine helpers (markFailed/Uploading/Processing/Ready/Generating across publishing.ts + dubbing.ts) gained `WHERE status [= source \| != terminal]` compare-and-swap guards so a late-arriving error can no longer overwrite a 'live'/'ready' row. videos.insert switched from multipart Buffer to YouTube's resumable upload protocol — source ReadableStream pipes straight to YouTube via fetch's `duplex: 'half'`, no full-file buffering, removes the 256MB cap and 200MB-RAM-pin. |
| 8.3 | **Encryption-key independence + crypto hygiene** — split ENCRYPTION_KEY from AUTH_SECRET so a session-token compromise no longer implies an OAuth-token-at-rest compromise | ✅ | Audit C6. crypto.getKey() now prefers ENCRYPTION_KEY when set; falls back to AUTH_SECRET with a one-time per-process console.warn so existing prod can soft-migrate (set ENCRYPTION_KEY=$AUTH_SECRET, then rotate AUTH_SECRET fresh) without breaking encrypted OAuth tokens. Throws when neither is set (unchanged). 4 new tests assert key independence: ENCRYPTION_KEY-only is silent, fallback warns once across N calls, both-unset throws, ciphertexts under the two keys are mutually undecryptable. |
| 8.4 | **Observability + cost + UX polish sweep** — 13 medium-severity findings covering structured logging, prompt cache, cron observability, modal a11y, optimistic-delete safety, mobile scroll affordance, dead code | ✅ | Audit M1 + M2 + M3 + M4 + M5 + M6 + M7 + M8 + M9 + M13 + m2 + m4 + m5 + m7 + m8 + m9 + m10. Sweeps: (a) ~262 console.error → logger.error across 147 server files so request_id/user_id/workspace_id propagate to log search, (b) Anthropic `cache: true` enabled on the bulk-loop generators (comment-triage, cannibalization, retention-predictor, fix-the-dip) — ~90% cost reduction on the static system prompts, (c) both crons (run-workflows + poll-publishing) gained start/done/error structured log lines with duration_ms + result counts. Discrete fixes: (d) PublishToYoutubeModal gets role=dialog + aria-modal + escape-to-close + backdrop click + focus management + form-state reset on every open, (e) /api/publishing/[id] GET no longer auto-polls YouTube — explicit POST /api/publishing/[id]/poll endpoint for the modal so quota burn is bounded, (f) webhook fan-out concurrency capped at 5 + every kind (including generic) gets the SSRF block, (g) optimistic deletes on /cannibalization + /ask-studio now check res.ok before mutating local state, (h) <PageSkeleton> + <InlinePageSkeleton> shimmer placeholders wired into /spend + /ask-studio, (i) IP-spoof-resistant `getClientIP` (cf-connecting-ip → x-vercel-forwarded-for → LAST entry of X-Forwarded-For), (j) migration 0037 adds composite indexes for the most-common list-endpoint query shapes (ab_tests, critic_panels, published_videos), (k) mobile horizontal-scroll fade affordance on data tables, (l) two streaming routes migrated to the documented [ERROR: msg] sentinel pattern, (m) workflow-rules SELECT capped at 200 (no scaling cliff for workspaces with thousands of rules), (n) three dead components removed (PageHeader, AnalyticsPanel, StreamingText), (o) v1 localStorage fallback dropped from getFeatureDefaultModelId. |
| 8.5 | **AI improvement loop — post-publish prediction outcomes** — close the loop on Phase 4.2's retention predictor by storing actual-vs-predicted deltas after each video's first 14 days live, feeding the worst predictions back as few-shot examples so the model gets sharper on this user's specific channel over time | ✅ | Migration 0040. New `prediction_outcomes` table linked via published_videos.project_id (predictions → published_videos → video_analytics). Pure delta-metrics math in [src/lib/prediction-outcomes.ts](src/lib/prediction-outcomes.ts) — `interpolateCurve` + `computeDeltaMetrics` (MAE pp, biggest miss position + direction, 11-sample per-segment delta) + `aggregateAccuracy` (hook/early/midroll/outro buckets with older-vs-recent half trend split). 16 tests cover the pure helpers. Daily cron `/api/cron/capture-prediction-outcomes` (03:15 UTC) finds (prediction, video_analytics) pairs where the video has been live ≥14d and inserts one outcome row per pair — idempotent via UNIQUE (workspace_id, retention_prediction_id) + ON CONFLICT DO NOTHING. `predictRetention` now prefers `findFewShotOutcomes` over raw `findFewShotExamples`; cold-start workspaces fall back transparently. Prompt builder shows `prior prediction:` + `miss: MAE Xpp ... we OVER/UNDERpredicted` lines for outcome rows + a leading NOTE telling the model "N of the examples are AUDITED OUTCOMES" so it calibrates toward the channel's actual delta pattern. Dashboard `PredictionAccuracyCard` (per the menu-organization rule: additive widget on existing `/dashboard`, no new top-level nav) auto-hides until the first outcome lands, then renders 4 bucket tiles with MAE/bias/sample-count + trend arrows + a one-line headline ("Hook predictions are getting sharper; midroll is drifting"). |
| 8.6 | **Second-pass hardening sweep** — four-agent QA review of Phase 8.1–8.5 surfaced 1 P0 + 16 P1s; all closed across five focused commits | ✅ | **8.6.1** SSRF DNS-pinning: new `resolveAndPinSafeUrl` does dns.lookup + validates every resolved address + returns an undici Dispatcher pinned to the validated set so undici can't be re-resolved into AWS IMDS / 127.0.0.1 / RFC1918 between validation and fetch. Wired into publishing source-video + thumbnail + webhooks fan-out + competitor thumbnail-analyze (with 8 MB buffer cap). IPv6 coverage gaps closed: fully-expanded loopback/unspecified, IPv4-compatible legacy `::a.b.c.d`, NAT64 64:ff9b::/96 + 64:ff9b:1::/48, zone-suffix variants, uppercase. undici@^6.24.1 added as explicit dep. +21 tests. **8.6.2** Phase 8.5 fixes: extracted `normalizeCurve` to `retention-curve-utils.ts` to break a circular import; added `rp.created_at < pv.created_at` to capture cron so retroactive predictions don't pollute the training signal; mixed cold-start tops up few-shot with raw analytics when outcomes < MAX; `findFewShotOutcomes` uses `DISTINCT ON (po.youtube_video_id)` to avoid 3-iteration-same-video duplication; `parseDeltaMetrics` clamps mae_pct to [0,100], position to [0,1], delta to [-1,1] + drops NaN/Infinity; single-outcome dashboard headline switched from "steady" to "First captured outcome — trend lines will appear once a second video crosses 14 days." +6 tests. **8.6.3** Publishing pipeline: `pollPublishStatus` "still processing" UPDATE gained `WHERE status = 'processing'` so concurrent pollers can't drift state backwards after a `live` flip; `markFailed` (publishing + dubbing) now `WHERE status NOT IN (terminal, 'failed')` so a second error path can't overwrite the first error message; idempotency-key INSERT race catches the UNIQUE-violation and re-resolves to the winning row; `parseIdempotencyKey` enforces unreserved-URI charset per RFC 3986 §2.3 (extracted to publishing-types.ts, +13 tests). **8.6.4** Removed inline `UNIQUE` from db.ts ensureCompetitorSchema CREATE TABLE so fresh installs match post-0038 schema; new `tests/auth-gates.test.ts` (13 tests) calls each Phase 8.1 previously-leaking handler with no session and asserts 401 — catches any future refactor that drops apiRoute.authed. **8.6.5** Swept ~25 routes still leaking `err.message` in 5xx through `domainErrorResponse` (helper from Phase 6.2) — every server route now logs at ERROR with full detail and returns generic 502 + domain-specific fallback message; thumbnails/image gained knownPatterns for Kie.ai env-var-missing (500) and "temporarily unavailable" (503). PublishToYoutubeModal gained Tab-trap so focus wraps inside the dialog — completes the keyboard half of `aria-modal="true"`. Crypto fallback warning routed through `logger.warn` (was `console.warn`) so structured-log alerts pick it up; cyclic-import justification was unfounded. Net: 27 files +152/-105 in 8.6.5 alone. Full suite 665/665. |

## Phase 9 — Analytics depth ✅

Promoted on 2026-05-05 after Phase 8 fully shipped. Theme: depth-not-breadth on the analytics surface. Phase 2.3 ingested per-video stats; Phase 4 added the predictor + dip detector + cannibalization + ask-studio; Phase 5.3 added cross-competitor signals. The gap was **trajectory**, **attribution**, and **proactive synthesis** — areas where one snapshot per video isn't enough. Seven sub-items, foundation first. Phase 9.8 closes the QA review's findings (2 P0 + 8 P1 + 6 P2 across 3 commits).

**Migration map (Phase 9):** 0041 (`video_analytics_history`), 0042 (`video_analytics.traffic_source_breakdown` column), 0043 (`video_search_terms`), 0044 (`video_format_tags`), 0045 (`video_breakout_fires`), 0046 (`insight_digests` + `weekly_digest_*` columns on `workspaces`), 0047 (`saved_catalog_views`), 0048 (drop redundant `video_analytics_history` per-video index).

| Sub | Topic | Status | Notes |
|---|---|---|---|
| 9.1 | **Time-series snapshots** — `video_analytics_history` append-only table; cron snapshots tracked videos every 6h. Foundation for 9.4 / 9.5 / 9.6 — most other items get sharper or cheaper if it ships first. | 🚧 | Append-only, no upsert. Composite PK `(workspace_id, youtube_video_id, captured_at)`. Cron walks recently-published videos (default 14d window) and snapshots stats for each. Pure helpers: `computeViewVelocityPerHour`, `bucketByDay` for the dashboard. The snapshot lib stays cheap by skipping the retention curve (only the live curve in `video_analytics` matters; the snapshot table is for trajectory of view-count / CTR / AVP / subs). |
| 9.2 | **Traffic source ingest + dashboard slice** — YouTube Analytics `trafficSourceType` per video → new `traffic_source_breakdown jsonb` column on `video_analytics`. Surfaces Suggested-feed / Search / Browse / External / Shorts-feed share. Highest-leverage single signal of channel health. | ✅ | Migration 0042. `syncVideoAnalytics` extends the Analytics API call with `dimensions=insightTrafficSourceType, metrics=views`; soft-fails to null on any HTTP error or missing-scope. Pure parsers (`parseTrafficSourceRows` + `trafficSourcePercentages`) in [src/lib/youtube-analytics.ts](src/lib/youtube-analytics.ts) handle case normalisation + duplicate-source summing + non-finite drop. Workspace-aggregator [src/lib/traffic-source-summary.ts](src/lib/traffic-source-summary.ts) merges per-video breakdowns into 30d-recent vs 30d-prior windows, computes per-source percentage deltas sorted by magnitude. New `TrafficSourceCard` on `/dashboard` (auto-hides when no videos have a breakdown yet) shows top-5 sources with progress bars, percentage shares, and ±pp deltas; headline calls out the biggest mover ("Suggested-feed share is +14.2pp vs the prior month"). Suggested-rising = green border; Suggested-falling >2pp = red border. 16 tests. |
| 9.3 | **Search query tracking** — `searchTerm` dimension via Analytics API → new `video_search_terms` table. Surfaces high-impression / low-CTR queries with AI-suggested title rewrites. | ✅ | Migration 0043. `syncVideoAnalytics` lazy-imports + fans out to `syncSearchTermsForVideo` after the live-row upsert; pulls `insightTrafficSourceDetail` filtered to `YT_SEARCH`, parses each (term, impressions, views) tuple, recomputes CTR (the API doesn't return it for the detail dimension), lowercases the term for stable trajectory grouping, caps writes at 50 rows per sync. Pure scoring in [src/lib/search-terms.ts](src/lib/search-terms.ts) — `scoreSeoOpportunities` combines log-scaled impression weight × CTR shortfall vs a configurable baseline (default 5%); normalises score to [0, 1]. New `seo-title-rewrite` AppFeature registered in `ai-models.ts` (defaults to Sonnet, workspace-overridable per Phase 6.2's resolver). `suggestTitleRewrite` asks the model for a rewrite that targets the top-N opportunities — strict JSON output, byte-stable system prompt for cache hits, ≤100-char enforcement, anti-clickbait rules baked in. New routes: `GET /api/seo/opportunities?youtubeVideoId=...` returns scored opportunities; `POST /api/seo/rewrite-title` returns the AI suggestion. Both tenant-scoped via the video_analytics row's workspace_id. 12 tests on the parser + scorer. |
| 9.4 | **Format / topic auto-tag attribution** — every published video gets a detected format tag (explainer / list / story / tutorial / commentary / interview) via cached AI call over title + script. Aggregate performance per tag. | ✅ | Migration 0044 (one row per video, PK on workspace + youtube_video_id). Pure helpers in [src/lib/format-tags.ts](src/lib/format-tags.ts) — `parseFormatTagOutput` (lowercases + de-dupes + caps topics at 5; clamps confidence to [0,1]; falls back to 'other' for unknown formats), `aggregateFormatStats` (per-format mean AVP/CTR/views with null handling). 9-format enum: explainer / list / story / tutorial / commentary / interview / vlog / showcase / other. New `video-format-tagger` AppFeature (defaults to Haiku — small prompt, cost-sensitive). Byte-stable system prompt + cache:true so re-tagging is cheap. Daily cron `/api/cron/tag-video-formats` (02:30 UTC) sweeps untagged published videos, bounded at 100/run so backlog unwinds gradually without spiking spend. Dashboard `FormatAttributionCard` (auto-hides until ≥3 buckets exist) shows per-format mean AVP/CTR vs the channel-weighted-mean baseline; headline calls out the spread ("Tutorials run +8.4pp AVP vs your channel mean; stories run -4.2pp"). 14 tests on the parser + aggregator. |
| 9.5 | **Breakout / anomaly detector** — sliding-window over the 9.1 time-series. Fires `video_breakout_detected` when a video's velocity exceeds the channel's 90th percentile in its first 48h. | ✅ | Migration 0045 (`video_breakout_fires` log table; UNIQUE on workspace+video gives one-fire-per-video idempotency). Pure helpers in [src/lib/breakout-detector.ts](src/lib/breakout-detector.ts) — `hoursBetween`, `computeFirstWindowVelocity` (window-bounded velocity over the first N hours since publish), `qualifiesAsBreakout` (percentile ≥ 0.9 AND strictly > p90 — strict-greater prevents flat-channel self-fires; refuses when population < 5). Cron `/api/cron/detect-breakouts` (every 6h at :45, staggered after the snapshot cron) walks recently-published videos, computes the channel's first-48h velocity population over the last 90 days, fires for qualifiers via `video_breakout_detected` event into both the workflow trigger registry AND the webhook fan-out. New event registered in `WORKFLOW_TRIGGER_EVENTS` + `WEBHOOK_EVENT_TYPES` so users can build rules ("when breakout, post to Slack" / "when breakout, run cannibalization scan"). 12 tests cover the math (window filtering, percentile threshold, tie-exclusion). |
| 9.6 | **Weekly insight digest (AI synthesis)** — Mondays 09:00 UTC, AI scans the workspace's 9.1+9.2+9.4+9.5 data and writes a 1-page "what changed, why, three concrete next moves" summary. Delivered to Slack/Discord AND email. | ✅ | Migration 0046 (`insight_digests` artifact table — PK on workspace+week_start; idempotent re-runs UPSERT — plus `weekly_digest_enabled` boolean and `weekly_digest_email_recipients` text on the `workspaces` table). [src/lib/weekly-digest.ts](src/lib/weekly-digest.ts) has pure helpers (`getDigestWeekWindow` for the Mon-00:00-UTC → next-Mon-00:00-UTC window math with prior-week comparison; `markdownToBasicHtml` minimal whitelist-only renderer for the email body — no parser dep, only h1/h2/p/ul/li/strong with inline styles; `summariseInputs` for the webhook detail field) plus DB orchestration (`assembleDigestInputs` joins 9.1 trajectories + 9.5 breakouts + AB-test conclusions, `runWeeklyDigestSweep` per-workspace AI call → persist → fan out). New `weekly-insight-digest` AppFeature defaults to Sonnet. Skips workspaces with zero activity (no views, no breakouts, no concluded tests) — sending an empty digest trains the user to ignore it. Cron `/api/cron/weekly-insight-digest` Mondays 09:00 UTC. Permalink page `/insights/[week]` is a server component reading `insight_digests` directly (bookmarkable, Slack-able). New `weekly_insight_digest` webhook event. Email delivery uses existing `@sendgrid/mail` via [src/lib/email.ts](src/lib/email.ts). Inline `DigestPreferencesPanel` on the permalink page lets users toggle opt-in + override email recipients without leaving for /settings; backed by GET/POST `/api/insights/preferences` with strict comma-separated email-list validation + 1KB cap. 10 tests covering the date math, Markdown→HTML rendering edge cases, and the summary-line formatter. |
| 9.7 | **Catalog explorer page** — power-user sortable table over every video. Filter chips, saved views. | ✅ | Migration 0047 (`saved_catalog_views`). Built at `/insights/catalog` (per the user's "option B" — sibling to the digest permalink, no new top-level nav; cross-linked from the dashboard's Underperformers section + the digest's "Browse all videos →" link). [src/lib/catalog-explorer.ts](src/lib/catalog-explorer.ts) has pure helpers (`normaliseFilter` / `normaliseSort` / `parseSavedViewName` — all defensive parsers that the URL-query path AND the saved-view-loader path flow through, so the DB query never sees garbage; `DURATION_BUCKET_RANGES` for shorts/short/mid/long buckets). DB read uses `sql.query()` with parameterised filter clauses + an enum-validated raw ORDER BY (template-tag interpolation can't compose ORDER BY direction). Joins `video_analytics` + `channels` + `video_format_tags` (9.4) + `video_breakout_fires` (9.5) for the format chip + breakout badge. Three routes: `POST /api/catalog/videos`, `GET/POST /api/catalog/saved-views`, `DELETE /api/catalog/saved-views/[id]` — all `apiRoute.authed` and tenant-scoped. The page itself is a single client component with a sticky filter sidebar (channel multi-select, format chips, duration buckets, published-date range, AVP/CTR ranges, only-breakouts toggle), a sortable column header row, paginated 50/page, and a saved-views section with inline create/delete. 15 tests cover every defensive-parse branch (UUID validation, format/duration enum guards, date format rejection, range clamping, strict-boolean for onlyBreakouts). Bulk actions deliberately deferred — the user can request them once they know which actions belong here. |
| 9.8 | **Phase 9 QA second-pass** — four-agent QA review surfaced 2 P0 + 8 P1 + ~15 P2; fixed in three focused commits. | ✅ | **9.8.1** XSS in `markdownToBasicHtml` — was security theatre with no actual escape; AI output flowed verbatim into `dangerouslySetInnerHTML` and email HTML. New `escapeHtml` helper applied per-line BEFORE the bold-regex emits its own `<strong>` tags; bold regex tightened to `[^*]+?` so `**` can't be smuggled. +8 XSS-hardening tests. **9.8.1 (cont)** Breakout detector N+1 (was 20K+ round-trips per cron at scale; would exceed the 300s timeout on multi-channel workspaces) rewritten as a single CTE — `velocities` + `channel_p90` + final SELECT with `CUME_DIST()` percentile. Dispatch awaited inline (Vercel serverless freeze-after-response would otherwise drop notifications). New `slack-escape.ts` (`escapeSlackText` for Slack mrkdwn link smuggling, `sanitizeForPrompt` for AI prompt injection). +10 tests. **9.8.2** Digest cron double-fire delivered twice — `RETURNING (xmax = 0) AS inserted` gates webhook + email fan-out so retries short-circuit; populated `delivery_status` JSONB column (existed since 0046, never written). Email regex consolidated into `src/lib/email-list.ts` (route + cron now agree on one parser; +13 tests). Search-term per-sync timestamp drift fixed — one `capturedAt` per sync run so the (workspace, video, term, captured_at) PK actually dedups. Title prompt-injection sanitization wired into the digest builder. Email loop swapped sequential → `Promise.allSettled`. **9.8.3** Migration 0048 drops the redundant per-video index on `video_analytics_history` (duplicated the PK; pure write amplification). Saved-view delete gained a `confirm()` step (workspace-wide views — fat-finger by Alice deletes Bob's). Catalog `colSpan={11}` → `={10}` (off-by-one). Magic numbers documented (`PER_SYNC_ROW_CAP`, `CATALOG_PAGE_SIZE`). Format-tagger PG TEXT[] write switched from manual literal to parameterised array via `sql.query` — correct-by-construction, no more "safe by sanitisation" string escape. Digest's per-window views aggregation switched from `MAX(views) - MIN(views)` to `last - first` via `array_agg(views ORDER BY captured_at)` — old form over-counted on volatile YouTube re-counts. Full suite 791/791. |

## Phase 10 — Cross-device history sync ✅

Promoted on 2026-05-07 in response to a user report ("history shows what I did 3 days ago on this PC, not what I did yesterday on a different PC"). The seven generator history panels (script, ideas, voiceover, seo, thumbnail, qa, production_doc) all stored entries in browser `localStorage` only — per-device, no recovery. Single sub-item, ships in one PR.

**Migration map (Phase 10):** 0049 (`user_history` polymorphic table — single composite index + partial unique index for migration idempotency).

| Sub | Topic | Status | Notes |
|---|---|---|---|
| 10.1 | **Server-synced history for the seven generator panels** — entries persist per-`(workspace, collaborator)` so a teammate's history stays separate, but follow the user across devices. localStorage stays as a write-through cache for instant paint + offline tolerance. | ✅ | Migration 0049 (single polymorphic `user_history` table — `kind` discriminator + `payload JSONB`). Server lib in [src/lib/user-history.ts](src/lib/user-history.ts) with `KIND_CAPS` mirroring the legacy `MAX_*_ENTRIES` and a `MAX_PAYLOAD_BYTES` ceiling (256 KB) → 413 on oversize. Three routes under `/api/history`: `GET ?kind=` (list), `POST` (insert + auto-trim beyond cap), `PATCH /[id]` (replace payload — for thumbnail/production-doc image-attach), `DELETE /[id]` (scoped delete returns 404 cross-scope), `POST /clear` (wipe one kind). [src/lib/history.ts](src/lib/history.ts) refactored: every `get*/save*/delete*/clear*` async, fetches from server, write-through to `localStorage` cache; new `get*HistoryCached()` sync variants for autocomplete + initial paint. One-shot localStorage→server upload runs lazily on first call to each `get*History()` after deploy — keyed by the legacy `client_id` for idempotent retry via the partial unique index. Updated 7 consumer pages + ScriptVoiceoverPanel + the two voiceover-URL pre-fill spots in production-doc/video-studio. 30+ tests (pure-logic + auth gates + input validation with mocked sql + cross-scope DELETE assertions). |

## Phase 12 — Team Hub (owner-side cockpit) ✅

Promoted on 2026-05-09 in response to a user report ("it's very hard to manage the team — narrators, video editors — across too many pages"). One unified surface that surfaces every team member with all of their tasks regardless of status, plus the team-member's own POV preview, plus an audited "Act as them" escalation. Single sub-item, ships in one PR (sequenced as ten reviewable commits).

**Migration map (Phase 12):** 0050 (`team_hub_audit_log` + `posted_by_owner` BOOLEAN on `narration_take_comments` / `review_comments` / `narrator_comments`).

| Sub | Topic | Status | Notes |
|---|---|---|---|
| 12.1 | **Owner-side team cockpit at `/team-hub`** — three-pane workspace (left rail roster grouped by role + filter chips, middle command center with Tasks/Their view/Activity/Settings tabs, animated right pane that mounts heavy embedded surfaces). Per-role command-center templates for narrator / video editor / reviewer / channel editor. POV-preview iframe of the team member's own portal. Always-audited "Act as them" escalation on take-comments. Add Person modal with role picker (Option 3c hybrid for channel editors). | ✅ | Migration 0050 (`team_hub_audit_log` + `posted_by_owner` flag on three comment tables). New lib modules: [src/lib/team-hub-types.ts](src/lib/team-hub-types.ts) (URL state types + pure helpers, 35 tests), [src/lib/team-hub-db.ts](src/lib/team-hub-db.ts) (workspace-scoped roster query unioning collaborators + channel_editors), [src/lib/team-hub-tasks-db.ts](src/lib/team-hub-tasks-db.ts) (per-role task queries — narrator/editor/reviewer/channel-editor), [src/lib/team-hub-activity-db.ts](src/lib/team-hub-activity-db.ts) (per-collaborator activity feed unioning takes / take comments / review versions / review comments / audit log), [src/lib/team-act-as.ts](src/lib/team-act-as.ts) (`actAsCollaborator(ctx, fn)` helper with workspace tenancy gate + always-audit lifecycle, 9 tests). New routes under `/api/team-hub/*` (roster + per-role tasks + take detail + workspace-scoped editor PATCH + audited act-as POST/PATCH for take comments). New page at [src/app/(app)/team-hub/page.tsx](src/app/(app)/team-hub/page.tsx) with URL-driven state (`?person=<id>&tab=<tab>&surface=<kind>:<uuid>`). Components in `src/components/team-hub/` (LeftRail, MiddlePane, RightPane, SurfaceMounter, NarratorTasksTab, EditorTasksTab, ReviewerTasksTab, ChannelEditorTasksTab, TheirViewTab, ActivityTab, SettingsTab, AddPersonModal). Heavy embedded components (TakeReview, ReviewPage, EditorTab) reused as-is — verified prop-driven, no refactor needed. Sidebar nav gets a "Team Hub" entry above legacy "Team"; `/narrators` gets a sunset banner pointing at the hub (page scheduled for removal after a two-week verification window per the plan). |

## Phase 13 — Niche finder 🚧

Promoted on 2026-05-12 in response to a user request ("add a robust niche finder like vidIQ and nexlev"). Council-revised plan in [_plans/2026-05-12-niche-finder.md](_plans/2026-05-12-niche-finder.md): niche stays as the user-facing unit but concept clusters are the engine underneath so monetization signals come from sampled real videos, not RPM-table-times-regex. Three sub-items planned; only 13.1 (v0.5 deep-dive) shipped so far.

**Migration map (Phase 13):** 0055 (`niche_reports`, `niche_finder_api_cache`).

| Sub | Topic | Status | Notes |
|---|---|---|---|
| 13.1 | **v0.5 — falsifiable scorer + deep-dive on a typed niche** | ✅ | Migration 0055 (two tables: `niche_reports` keyed `(workspace_id, slug)` + shared `niche_finder_api_cache` for the 7-day YouTube API cache). Pure scoring helpers in [src/lib/niche-finder/scoring/](src/lib/niche-finder/scoring/) — `demand` (log-normalised views + recent-publish cadence + autocomplete diversity), `supply` (top-channel concentration + median age + new-entrant rate + channel count, where higher = more saturated), `monetization` (mid-roll-eligible share × sponsor density × merch presence × category RPM prior; output is a per-1,000-views USD range with confidence band, never a point estimate), `fit` (delegated to a single AI call). Rollup is sample-weighted; combined sort score = 0.3·demand + 0.3·(1 − supply) + 0.3·monetization + 0.1·fit. RPM prior table in [src/lib/niche-finder/rpm-priors.ts](src/lib/niche-finder/rpm-priors.ts) with public-source citations and a 12-month staleness warn. YouTube Suggest fetcher with 350ms self-throttle + 7-day cache + graceful empty-array fallback. YouTube Data API wrappers (`searchVideosForCluster` = 100 quota units, `fetchVideosBatch` + `fetchChannelsBatch` = 1 unit per 50 IDs) with cached-fetch + structured quota logging. AI cluster-mapper constrained to harvested terms (Executor's prod-failure-mode guard against hallucinated niches). Heuristic clustering fallback when AI fails. Strategy memo via Sonnet with prompt caching + spend logging. Orchestrator [src/lib/niche-finder/run-deep-dive.ts](src/lib/niche-finder/run-deep-dive.ts) chains Suggest → cluster → harvest → score → rollup → synthesize → upsert in ~506 quota units worst case. Routes `POST /api/niche-finder/deep-dive` and `GET /api/niche-finder/niches/[slug]`, both `apiRoute.authed`. Server-rendered deep-dive at `/insights/niches/[slug]` with four plain-English score chips ("How many people want this / How crowded it is / How much money it makes / How well it fits you" per the Outsider council pass), cluster breakdown, AI memo through the hardened `markdownToBasicHtml`, regenerate button. Entry form at `/insights/niches`. New "Niche finder" sidebar entry under Grow. Two new `AppFeature` registrations (`niche-cluster-map` Haiku-default, `niche-deep-dive` Sonnet-default) with the full workspace/section/feature override stack. **80 niche-finder tests** + auth-gate regression + 1152 full-suite (all green). |
| 13.2 | **v1 — four discovery modes (A interests / B channel-paste / C category browser / D outliers)** | ✅ | Migration 0058 (`niche_discoveries` polymorphic table — UNIQUE on (workspace, kind, input_hash) gives idempotent re-runs; 7-day TTL collapse to a single quota burn). Four orchestrators in `src/lib/niche-finder/`: `discover-from-interests` (AI proposes 8 niches from operator interests, each scored with real YouTube data; ~820 quota units / ~12 runs/day on default quota), `discover-from-channel` (paste a channel URL, we fetch 50 recent uploads, cluster titles, score the channel's matched videos per cluster — ~4 quota units, much cheaper than A), `discover-from-category` (curated 12-category taxonomy in `categories.ts`, each with 4-6 sub-niches scored — ~500 units/category), `outliers.ts` (views ÷ max(subs, 1000), with `underperformer / normal / breakout / viral` classification matching the existing `competitor-summary` thresholds). Four new routes under `/api/niche-finder/discover/{from-channel, from-interests, from-category}` + `/api/niche-finder/outliers`, all `apiRoute.authed` with auth-gate regression coverage. Tabbed discovery hub at `/insights/niches` (5 tabs: Type a niche / Interests / Channel / Categories / Outliers) replaces the v0.5 entry-only form. Discovery cards link into the v0.5 deep-dive flow on click. New `niche_finder_api_cache` (from migration 0055) is now shared across all five modes. 39 new tests (23 discoveries + 11 outliers + 5 auth-gates), full suite 1191/1191, typecheck and lint clean. Watchlist + weekly cron + flywheel hooks (originally bundled in this sub-item) deferred to 13.2.W / 13.2.F respectively. |
| 13.2.W | **Watchlist with weekly re-score cron** | ✅ | Migration 0059 (`niche_watchlist` — PK on workspace+slug, 26-snapshot JSONB history, configurable alarm_threshold defaulting to 0.10). Pure helpers in [src/lib/niche-finder/watchlist.ts](src/lib/niche-finder/watchlist.ts): `trimHistory`, `snapshotFromScores`, `detectScoreSpike` (delta = last − prev, fires when `|delta| ≥ threshold`, returns direction + trigger timestamp). Routes: `GET/POST /api/niche-finder/watchlist`, `DELETE /api/niche-finder/watchlist/[slug]`, all `apiRoute.authed`. POST seeds the first snapshot from the persisted `niche_reports` row if one exists so the sparkline isn't empty on day one. New `/insights/niches/watchlist` server-rendered page with inline-SVG `Sparkline` component (no charting lib), per-row 26-week sparkline + last-snapshot dimension labels + delta arrow. Save button on the v0.5 deep-dive shows "+ Watchlist" / "Watching ✓" state via a one-shot list probe. Cron at `/api/cron/rescore-niche-watchlist` (Sundays 04:00 UTC, CRON_SECRET-gated, wired in vercel.json) calls `runDeepDive(force=true)` per row, appends a snapshot, runs the score-spike detector, fires `niche_score_spike` into BOTH the workflow trigger registry and the webhook fan-out when a spike is detected. 16 watchlist tests + 4 new auth-gate cases. |
| 13.2.O | **Outlier filters + smart presets + saved searches** | ✅ | Migration 0060 (`niche_search_presets` per-workspace, UNIQUE on (workspace, name), 100-row cap enforced at write-time). Pure helpers in [src/lib/niche-finder/outlier-filters.ts](src/lib/niche-finder/outlier-filters.ts): 8 filter dimensions (format short/normal/long via duration buckets; channel size tiny/small/mid/large; min views; published-within window; min outlier score; title length punchy/medium/descriptive; consistent-winners-only flag derived from channels with ≥3 hits in the result set; 6 sort orders including subs-ascending for "smallest-channels-first" discovery). `filterAndSortOutliers` is fully client-side over the already-fetched array — dialling controls is instant, no extra quota. Nine built-in smart presets: Breakout shorts / Hidden gems / Sleeper hits / New voice rising / Long-form winners / Recently viral / Mid-roll friendly / Consistent winners / Punchy-title winners. User-saved presets via [presets-db.ts](src/lib/niche-finder/presets-db.ts) + routes `GET/POST /api/niche-finder/outliers/presets` and `DELETE .../[id]` (apiRoute.authed, workspace-scoped, UNIQUE collisions return 409 for in-place rename). UI components [OutlierFilterBar](src/components/niche-finder/OutlierFilterBar.tsx) (segmented-chip rows) and [OutlierPresetBar](src/components/niche-finder/OutlierPresetBar.tsx) with inline "+ Save current search" prompt. Empty-state when filters yield 0 matches has a one-click Reset. 41 filter tests + 3 new auth-gate cases. |
| 13.2.F | **Flywheel hooks (Expansionist's contribution)** | ✅ | New `niche_score_spike` registered in both `WORKFLOW_TRIGGER_EVENTS` and `WEBHOOK_EVENT_TYPES` so the existing workflow rules + webhook subscriptions can react to it. Producer fires from the 13.2.W rescore cron. New `GenerateIdeasButton` on the deep-dive: serialises the top 3 clusters + their top 5 video titles into sessionStorage, then routes to `/ideas?niche=<name>&from=niche-finder`. The existing `/ideas` page got a parallel prefill effect (mirroring the schedule-prefill pattern) that reads the niche + sessionStorage on mount, seeds the `niche` input, shows a toast, and clears the prefill so it doesn't haunt later sessions. Reuses the existing idea-generator infra end-to-end — no new AI calls, no new state stored server-side. |
| 13.3 | **Productization — open to other workspaces (gated on YouTube audit + derived-metric permission)** | ⏸ | YouTube Developer Policy effective 2026-06-01 forbids derived metrics without explicit audited permission. Phase 13.3 is gated on receipt of an approval letter from Google via the [Audit + Quota Extension form](https://support.google.com/youtube/contact/yt_api_form?hl=en). Adds per-workspace quota fairness caps. |

## Phase 14 — Production-doc → render pipeline polish ✅

Promoted on 2026-05-14 alongside the production-doc-fonts work. Theme: every visible knob between a finished script and a rendered MP4 is now per-channel-default + per-video-overrideable, and every font shown in the renderer is loaded deterministically instead of relying on whichever system font happens to be installed on the render machine. One sub-item so far; the voiceover-aligned scene timing and thumbnail-zoom plans (`_plans/2026-05-13-*.md`) cover adjacent work in the same area but ship as their own units.

**Migration map (Phase 14):** 0070 (`channels.visual_brand_kit`).

| Sub | Topic | Status | Notes |
|---|---|---|---|
| 14.1 | **Per-channel default visual brand kit + per-video override** — fonts (8-family curated set loaded via `@remotion/google-fonts`), colors, logo URL, channel display name; channel kit is the new default, production-doc page lets each video override it. | ✅ | Migration 0070 (`channels.visual_brand_kit JSONB NOT NULL DEFAULT '{}'`). Pure parser + resolver in [src/lib/channel-visual-brand-kit.ts](src/lib/channel-visual-brand-kit.ts) (font allowlist via `ALLOWED_FONT_FAMILIES`, six-digit-hex regex for colors, R2-host allowlist for `logoUrl`). [src/remotion/fonts.ts](src/remotion/fonts.ts) expanded from Inter + Patrick Hand to the eight-family curated set (Inter / Patrick Hand / Anton / Bebas Neue / Archivo Black / Caveat / Source Serif 4 / JetBrains Mono) with a single `loadGated` helper + `FONT_REGISTRY` + `resolveFontStack`. Three new API routes: `GET/PUT /api/channels/[id]/visual-brand-kit` and `POST /api/channels/[id]/visual-brand-kit/logo` (channel-scoped R2 presign, 2 MB cap, JPEG / PNG / WebP / SVG). New settings page at `/channel/[id]/visual-brand-kit` mirrors the existing script brand-kit page; shared `VisualBrandKitFields` (FontPicker / ColorField / LogoDropzone) used by both the channel settings and the production-doc override panel. `ProductionDocHistoryEntry` (Phase 10's `user_history`) gained `visualBrandKitOverride` — no new table, override persists per-doc and cross-device via the existing PATCH path. Production-doc page composes the merged kit via `resolveBrandKitForRender(channel, override)` + the legacy `VideoPreviewBrandBar` localStorage tweak on top, then passes through `productionDocToVideoConfig`. Out-of-plan find: the experimental `/video-studio` `BrandTab` had hard-coded Space Grotesk / Oswald / DM Sans that were never bundled — its font list now reads from `FONT_REGISTRY` so the studio matches the production-doc renderer. 22 new tests in [tests/channel-visual-brand-kit.test.ts](tests/channel-visual-brand-kit.test.ts) cover the parser (font/hex/URL allowlists, version mismatch, unknown-key drop) + the resolver (channel-over-default, override-over-channel, font-stack expansion). Full suite 1433/1433 (1411 prior + 22 new). Plan + as-built deltas in [_plans/2026-05-13-production-doc-fonts.md](_plans/2026-05-13-production-doc-fonts.md). |

## Phase 11 — Owner-side editor-video upload + upload-path hardening ✅

Promoted on 2026-05-09 in response to a user report ("when I tried uploading via the editor's dashboard, I waited very long and the video was not uploaded"). Adds an owner-side upload box on the project's Editor tab so a video received off-platform (e.g. via Upwork) can be dropped straight into the review pipeline + comment surface, and hardens the long-standing failure modes that had made the editor's own upload feel like a hang. No new migration.

| Sub | Topic | Status | Notes |
|---|---|---|---|
| 11.1 | **Owner-side "Editor's finished video" upload + upload-pipeline hardening** — new POST/GET/PATCH `/api/projects/[id]/editor-uploads`, new section in `EditorTab` with compress/abort/cancel, plus root-cause fix for the editor-side hang. | ✅ | Root cause of the "long wait, never uploaded" was [src/lib/review-db.ts](src/lib/review-db.ts) `createProject` not threading `workspace_id`: after migration 0013 made `review_projects.workspace_id` `NOT NULL`, the editor's **first** upload hit a Postgres NOT NULL violation and returned 500 — but only after compression + thumbnail probe burned minutes in the browser, so it looked like a hang. Signature now requires `workspaceId`; both callers thread it through (editor-side from `assignment.workspace_id`, owner-side from `session.ws`). New shared helper [src/lib/upload-video-client.ts](src/lib/upload-video-client.ts) wraps compress + probe + presign + PUT + confirm with: 15-min hard timeout + 90-s stall detector on compress, 8-s timeout on the thumbnail probe (was unbounded — some MOV/MKV files never fired `onseeked`), `xhr.timeout` of 4h + 60-s stall detector on the R2 PUT, and a shared `AbortSignal` so a single Cancel button unwinds the whole pipeline. R2 presigned PUT TTL bumped 1h → 4h in [src/lib/r2.ts](src/lib/r2.ts) so a slow-but-progressing 4 GB upload no longer 403s mid-stream. New owner route [src/app/api/projects/[id]/editor-uploads/route.ts](src/app/api/projects/%5Bid%5D/editor-uploads/route.ts) is `apiRoute.authed`, scopes to `workspace_id`, picks the project's single active editor assignment (or fails with a clear message when there are zero or multiple), creates the `review_project` + `review_version` with `uploaded_by = "<editor name> (uploaded by owner)"` so the audit trail is honest. UI in [src/components/editor/EditorTab.tsx](src/components/editor/EditorTab.tsx) renders right under Production Doc, lists every uploaded version with Open-review link, supports cancel mid-compress + mid-PUT. Editor-side page [src/app/editor/[token]/[projectId]/page.tsx](src/app/editor/%5Btoken%5D/%5BprojectId%5D/page.tsx) refactored to use the same helper so cancel + timeouts are available there too. New `classifyXhrFailure` helper distinguishes ABORTED / PUT_TIMEOUT / PUT_STALL / PUT_URL_EXPIRED / PUT_CORS / PUT_REJECTED so toasts surface the actual problem instead of "network error". Hardening side-effects: `POST /api/review/projects` got an `apiRoute.authed` wrapper (was unauthenticated), `listProjects` now scopes to `workspace_id`. 8 tests on `classifyXhrFailure`. |

---

## How to update this file

When you **start** a sub-item: flip ⏸ → 🚧.
When you **finish** a sub-item:
1. Flip 🚧 → ✅
2. Add the migration number(s) it required
3. Commit the change to ROADMAP.md in the **same** PR that ships the feature.

When you **cancel** a sub-item: flip → ❌ with a one-line reason.

If a phase grows past ~10 sub-items or the file past ~200 lines, split it into its own `PHASE<N>_PLAN.md` (like Phase 1 did) and keep this file as the index.

**Don't invent new phase numbers or rename sub-items mid-flight.** New work goes under "Beyond Phase 4" until promoted by an explicit decision.
