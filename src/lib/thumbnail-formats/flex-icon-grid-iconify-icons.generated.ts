/**
 * Flex Icon Grid — Iconify icon registry (auto-generated).
 *
 * GENERATED FILE — do not edit by hand. Populate by running:
 *
 *     npx tsx scripts/download-flex-icon-grid-iconify-icons.ts
 *
 * The script fetches additional icon sets from the Iconify CDN
 * (https://iconify.design — most sets are MIT or Apache 2.0) and
 * overwrites this file with the entries. `flex-icon-grid-icons.ts`
 * spreads `ICONIFY_ICONS` into `ICON_REGISTRY`, so re-running the
 * script with a different icon list updates the picker on the next
 * render without any other wiring.
 *
 * The `.generated.ts` suffix is read by GitHub's linguist via
 * `.gitattributes` (`*.generated.ts linguist-generated=true`), which
 * collapses regenerated diffs in PR reviews — keeps the noise out of
 * the code-review surface area.
 *
 * Adding an icon
 *   Edit the curated list in
 *   `scripts/download-flex-icon-grid-iconify-icons.ts` and re-run.
 *   Each entry pairs an Iconify `<prefix>:<name>` (e.g.
 *   `heroicons:academic-cap`) with a panel label + category.
 *
 * License posture
 *   Iconify aggregates icon sets under their original licenses (most
 *   are MIT or Apache 2.0). The default curated list in the download
 *   script targets MIT/Apache sets only. If you add a set with a
 *   different license, ensure your use complies — this file ships
 *   under the same terms as the underlying sets.
 */

import type { IconEntry } from './flex-icon-grid-icons';

/** Auto-generated entries. Empty until the download script runs. */
export const ICONIFY_ICONS: readonly IconEntry[] = [];
