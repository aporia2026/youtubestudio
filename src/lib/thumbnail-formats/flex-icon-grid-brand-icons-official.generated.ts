/**
 * Flex Icon Grid — official brand icon registry (auto-generated).
 *
 * GENERATED FILE — do not edit by hand. Populate by running:
 *
 *     npx tsx scripts/download-flex-icon-grid-brand-icons.ts
 *
 * The script fetches Simple Icons (https://simpleicons.org, CC0-licensed
 * SVG path data) for a curated list of major brands and overwrites this
 * file with the entries. `flex-icon-grid-brand-icons.ts` imports
 * `OFFICIAL_BRAND_ICONS` and spreads it into `BRAND_ICONS`, so re-running
 * the script with a different brand list updates the panel's icon picker
 * on the next render without any other wiring.
 *
 * The `.generated.ts` suffix is read by GitHub's linguist via
 * `.gitattributes` (`*.generated.ts linguist-generated=true`), which
 * collapses regenerated-content diffs by default in PR reviews — keeps
 * the noise out of code-review surface area.
 *
 * Trademark posture: Simple Icons distributes the SVG path data as
 * CC0, but the underlying brand logos remain trademarks of their
 * respective owners. Embedding a brand logo in a thumbnail about that
 * brand is generally fair use under YouTube's policy; embedding a
 * competitor's logo in your own promotional material usually is not.
 * The script and this file are tools — the caller is responsible for
 * ensuring their specific use is legitimate.
 */

import type { IconEntry } from './flex-icon-grid-icons';

/** Auto-generated entries. Empty until the download script runs. */
export const OFFICIAL_BRAND_ICONS: readonly IconEntry[] = [];
